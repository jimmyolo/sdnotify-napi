# sdnotify-napi

> another sd-notify native binding

## Requirements

- systemd support
- node.js >= 16.0
- systemd dev files
  - debian: `apt install libsystemd-dev`
  - redhat: `yum install systemd-devel`

## Usage
